# Web framework
fastapi==0.121.1
starlette>=0.49.1  # patched Starlette (DoS / multipart advisories)
uvicorn[standard]==0.27.1

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# Security
python-jose[cryptography]==3.4.0  # CVE-2024-33663/33664 fixes
cryptography>=42.0.0  # used directly for Fernet token encryption
# NOTE: python-jose pins pyasn1<0.5, so the pyasn1 advisory (GHSA-jr27-m4p2-rc6r)
# cannot be cleared without replacing python-jose (e.g. with PyJWT). Low impact:
# pyasn1 is only exercised by jose's RSA/ECDSA paths; we sign with HS256.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart>=0.0.27  # DoS advisories fixed

# HTTP client for Open Banking
httpx==0.26.0

# Validation & settings
pydantic[email]==2.6.1
pydantic-settings==2.1.0

# Testing
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==4.1.0

# Development
black==26.3.1
ruff==0.2.0
mypy==1.8.0
pip-audit==2.7.3  # dependency vulnerability scanning
